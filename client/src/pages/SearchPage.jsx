import styled from 'styled-components';

const SearchPage = () => {
    return (
        <SearchPageBlock>
            검색 페이지
        </SearchPageBlock>
    )
}

const SearchPageBlock = styled.div`
    
`

export default SearchPage;