import styled from 'styled-components';

const RegisterPage = () => {
    return (
        <RegisterPageBlock>
            글쓰기 페이지
        </RegisterPageBlock>
    )
}

const RegisterPageBlock = styled.div`
    
`

export default RegisterPage;